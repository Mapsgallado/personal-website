---
export interface Props{
    classes: string,
    type?: "button" | "submit" | "reset" 
}
const { classes, type} = Astro.props
---

<button type={type ?? "button"} class={"btn " + classes }>
    <slot />
</button>

